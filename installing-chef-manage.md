# installing chef-manage in chef-server

    sudo chef-server-ctl install chef-manage
    sudo chef-server-ctl reconfigure
    sudo chef-manage-ctl reconfigure

And hit the server url, login with your user credentials.